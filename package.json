{
  "scripts": {
    "dev": "nodemon --watch extension -e ts,module.css --exec \"yarn build\"",
    "build": "esbuild ./extension/core/index.ts --bundle --minify --outfile=\"./extension/core/index.js\" --platform=browser"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "nodemon": "^3.1.3",
    "sass": "^1.77.5"
  },
  "dependencies": {
    "firebase": "^10.12.2"
  }
}
